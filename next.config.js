/** @type {import('next').NextConfig} */
const nextConfig = {
    // reactStrictMode : false,
    // webpack5: true,
    // webpack: (config) =>{
    //     config.respolve.fallback = {fs :false};
    //     return config
    // }
    
}

module.exports = nextConfig
